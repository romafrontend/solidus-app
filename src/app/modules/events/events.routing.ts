import {Routes} from '@angular/router';
import {EventsContainerComponent} from './components/events-container.component';

export const eventsRoutes: Routes = [
    {path: '', component: EventsContainerComponent}
];